{"id": "code-001", "text": "Write a Python function `longest_increasing_run(values: list[int]) -> list[int]` that\nreturns the longest contiguous strictly increasing run. If there are ties, return the\nfirst one. Include a docstring and type hints, and handle the empty list."}
{"id": "code-002", "text": "Refactor this so it is O(n) instead of O(n^2). Keep the function signature.\n\ndef find_pairs(items, target):\n    result = []\n    for i in range(len(items)):\n        for j in range(len(items)):\n            if i != j and items[i] + items[j] == target:\n                result.append((items[i], items[j]))\n    return result"}
{"id": "code-003", "text": "In Go, write an `http.HandlerFunc` that decodes a JSON body into a struct with fields\n`Name string` and `Age int`, returns 400 with a JSON error message if the body is\nmalformed or Age is negative, and otherwise responds 201 with the same struct echoed\nback. Use only the standard library."}
{"id": "code-004", "text": "Write a generic TypeScript `debounce` function. It should accept any function and a\ndelay in milliseconds, preserve the parameter types of the wrapped function, and expose\na `cancel()` method on the returned function. Show a short usage example."}
{"id": "code-005", "text": "Write a SQL query (PostgreSQL) that returns the five customers with the highest total\norder value in the last 90 days. Tables: `customers(id, name, email)` and\n`orders(id, customer_id, placed_at timestamptz, total_cents int, status text)`. Only\ncount orders whose status is 'paid'. Return name, email, and total in dollars."}
{"id": "code-006", "text": "Give me a bash script that lists every file under the current directory larger than\n100 MB that was modified in the last 7 days, sorted by size descending, printing the\nsize in human-readable form next to the path. It should not break on filenames with\nspaces."}
{"id": "code-007", "text": "Complete this class. `get` should return -1 for a missing key and both methods must be\nO(1). Do not import functools.\n\nfrom collections import OrderedDict\n\nclass LRUCache:\n    def __init__(self, capacity: int) -> None:\n        self.capacity = capacity\n        self._data: OrderedDict[int, int] = OrderedDict()\n\n    def get(self, key: int) -> int:\n        ...\n\n    def put(self, key: int, value: int) -> None:\n        ..."}
{"id": "code-008", "text": "Implement a worker pool in Go: a function `RunPool(ctx context.Context, jobs <-chan\nJob, workers int, fn func(context.Context, Job) error) error` that starts `workers`\ngoroutines, stops cleanly when the context is cancelled or the jobs channel closes,\nand returns the first error encountered. Use `errgroup` if you think it helps, but\nexplain the choice."}
{"id": "code-009", "text": "Write a type-safe event emitter in TypeScript. The constructor takes no arguments, but\nthe class is generic over an event map like `{ login: { userId: string }; logout:\nundefined }` so that `on('login', handler)` gives `handler` the right payload type and\n`emit('logout')` needs no second argument."}
{"id": "code-010", "text": "Using a window function, write a SQL query that ranks employees by salary within each\ndepartment and returns only the top three per department, including ties. Columns\navailable: employees(id, name, department_id, salary), departments(id, name)."}
{"id": "code-011", "text": "Write a Python function that parses lines like\n`2024-03-09T14:22:01Z WARN scheduler: job 4412 retried (attempt 3/5)` into a frozen\ndataclass with fields timestamp (datetime, UTC), level, component, and message. Use a\nsingle compiled regex, and raise a ValueError with the offending line if it does not\nmatch."}
{"id": "code-012", "text": "Write a POSIX sh script that rotates `/var/log/myapp.log`: rename the current file with\na numeric suffix, shift older ones up, gzip anything older than the newest rotation,\nand keep at most five archives. It must be safe to run from cron every hour even when\nthe log file does not exist."}
{"id": "code-013", "text": "This binary search sometimes loops forever or returns the wrong index. Find the bug,\nexplain it in one sentence, and give the corrected code.\n\ndef bsearch(a, x):\n    lo, hi = 0, len(a)\n    while lo < hi:\n        mid = (lo + hi) // 2\n        if a[mid] < x:\n            lo = mid\n        else:\n            hi = mid\n    return lo if lo < len(a) and a[lo] == x else -1"}
{"id": "code-014", "text": "In Go, read a CSV file with a header row from stdin, sum the numeric `amount` column\ngrouped by the `region` column, and print the totals sorted by region name. Handle\nquoted fields and skip rows where amount is not a valid float, counting them and\nreporting the count on stderr at the end."}
{"id": "code-015", "text": "Write a TypeScript `fetchWithRetry(url, init, { retries, baseDelayMs })` that retries on\nnetwork errors and 5xx responses with exponential backoff plus jitter, respects an\nAbortSignal passed in `init`, and never retries a 4xx. Return the final Response or\nthrow the last error."}
{"id": "code-016", "text": "Write a PostgreSQL migration that adds a partial index on `orders(customer_id)` for rows\nwhere status = 'open', and a CHECK constraint ensuring `total_cents >= 0`. Explain why\nthe index should be created CONCURRENTLY and what that means for running it inside a\ntransaction."}
{"id": "code-017", "text": "Implement an asyncio-compatible token bucket rate limiter in Python: `RateLimiter(rate:\nfloat, burst: int)` with `async def acquire()` that waits until a token is available.\nTokens refill continuously. Include a small demo that fires 20 coroutines through a\nlimiter of 5 per second and prints timestamps."}
{"id": "code-018", "text": "Split this function into smaller pieces with clear names. Behaviour must not change.\n\ndef process(path):\n    rows = []\n    with open(path) as f:\n        for line in f:\n            line = line.strip()\n            if not line or line.startswith(\"#\"):\n                continue\n            parts = line.split(\",\")\n            if len(parts) != 3:\n                continue\n            name, qty, price = parts\n            try:\n                qty = int(qty); price = float(price)\n            except ValueError:\n                continue\n            rows.append((name, qty, price))\n    total = 0.0\n    for name, qty, price in rows:\n        total += qty * price\n    top = sorted(rows, key=lambda r: r[1] * r[2], reverse=True)[:3]\n    print(f\"total={total:.2f}\")\n    for name, qty, price in top:\n        print(f\"{name}: {qty * price:.2f}\")\n    return total"}
{"id": "code-019", "text": "Write a generic ring buffer in Go (`RingBuffer[T]`) with `Push`, `Pop`, `Len`, and\n`Cap`. Pushing onto a full buffer overwrites the oldest element. Add table-driven tests\ncovering wraparound."}
{"id": "code-020", "text": "Shell one-liner or short pipeline: from an nginx access log in the default combined\nformat, print the ten most frequent client IPs with their counts, most frequent first."}
{"id": "code-021", "text": "Write a TypeScript function `parseDuration(input: string): number` that converts strings\nlike \"1h30m\", \"45s\", \"2h\", \"1h 2m 3s\" into seconds. Throw on anything else, including\nan empty string or repeated units. Add a handful of unit tests in vitest style."}
{"id": "code-022", "text": "Write pytest tests for this function. Cover the normal case, an empty list, a list with\na single element, negative numbers, and the claim that the result never mutates the\ninput.\n\ndef normalize(xs: list[float]) -> list[float]:\n    if not xs:\n        return []\n    lo, hi = min(xs), max(xs)\n    if hi == lo:\n        return [0.0 for _ in xs]\n    return [(x - lo) / (hi - lo) for x in xs]"}
{"id": "code-023", "text": "Write a SQL statement that deletes duplicate rows from `contacts(id, email, phone)` where\na duplicate means the same lowercased email, keeping the row with the smallest id. Then\nshow the query I can run first to preview which rows would be removed."}
{"id": "code-024", "text": "Build a small Python CLI with argparse: `csv2json input.csv --out out.json --nest`. Each\nCSV row becomes an object. When --nest is passed, dotted header names such as\n`address.city` become nested objects. Numbers that look numeric should be converted,\neverything else stays a string. Write the whole script."}
