"""The "Instrument" design tokens and their codegen (brief tab 5).

One `design/tokens.json` is rendered to both Lip Gloss constants (tui/theme)
and CSS custom properties (dashboard). Nothing here touches the runtime.
"""
