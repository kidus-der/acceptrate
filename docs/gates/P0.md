# P0 gate — environment and skeleton

**Gate:** `acceptrate bench --smoke` streams 128 tokens from both models and exits 0.

**Result: PASS** (2026-09-16, M4 Mac mini 16 GB, mlx 0.32.2, mlx-lm 0.31.3)

```
$ uv run acceptrate bench --smoke
draft  mlx-community/Llama-3.2-1B-Instruct-4bit: 128 tokens in 1.2s (109.3 tok/s, plain greedy)
target mlx-community/Llama-3.1-8B-Instruct-4bit: 128 tokens in 5.8s (21.9 tok/s, plain greedy)
smoke: ok
exit=0
```

These tok/s figures are *not* measurements — no guards, no warmup discard,
single run, includes Python overhead. They exist only to show both models
load and stream. The first trustworthy numbers come from P1.

Rough implied draft-cost ratio c ≈ 0.20, consistent with the brief's ~0.16.

Suite at the gate: 34 tests (28 CI-safe, 6 model-marked), ruff clean.
