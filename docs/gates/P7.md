# P7 gate — the report

**Gate:** `make reproduce` regenerates every published figure from raw
traces on a clean checkout.

**Result: PASS** (2026-09-16)

```
$ git clone <repo> clean-checkout && cd clean-checkout && uv sync
$ make reproduce
clean windows: 70290 (need 50000) … P3 gate: PASS
R² = 0.9704 over 192 speculative cells (gate > 0.85); closed form as written (v = 1): R² = -12.4825
P4 gate: PASS
  -> docs/figures/speedup_heatmap_Llama-3_2-1B-Instruct-4bit.{pdf,png}
  -> docs/figures/speedup_heatmap_Llama-3_2-1B-Instruct-8bit.{pdf,png}
  -> docs/figures/speedup_heatmap_Llama-3_2-3B-Instruct-4bit.{pdf,png}
  -> docs/figures/predicted_vs_measured.{pdf,png}
  -> docs/figures/residuals_vs_pressure.{pdf,png}
  -> docs/figures/alpha_by_position.{pdf,png}
exit=0
clean-checkout R² 0.970399 | committed R² 0.970399 | equal: True
```

The raw traces (1.9 MB of parquet + manifests under `traces/`) are in the
repository, so the clean checkout needs nothing but Python and `uv`.
`docs/report.md` is the report; `docs/gates/P0.md … P6.md` are its
evidence; `docs/findings/` holds the incidents worth keeping.

## Open items carried honestly

- The brief asks for the grid to be **re-run on a different day** and both
  published. Session 2's 1B-4bit draft already replicates session 1 within
  0.5% at every K; a second-day rerun is `make sweep-one DRAFT=…` three
  times plus `make reproduce`, and belongs in the report when it exists.
- The P5 gate failed twice and is recorded as such (`docs/gates/P5.md`);
  the phases after it were completed because nothing downstream consumes
  the adaptive result. Overruling that is the owner's decision.
- The distributional bound is loose at n = 400; a chi-square two-sample
  test would sharpen it.
- The CUDA / 3050 Ti backend that would turn one machine's v(K) into a
  bandwidth study is unbuilt; the seam for it (`backend/protocol.py`) is.
