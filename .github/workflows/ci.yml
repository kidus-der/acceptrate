name: ci

# Correctness gates only. Performance numbers never come from CI: shared
# runners are noisy and throttled. Tests marked 'model' or 'perf' are excluded.
# macOS ARM runners are free for public repos; this repo is private for now,
# so expect this to start running when it flips public.

on:
  push:
    branches: [main]
  pull_request:

jobs:
  correctness:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
      - run: uv python install 3.12
      - run: uv sync --frozen
      - run: uv run ruff check .
      - run: uv run ruff format --check .
      - run: uv run pytest -m "not model and not perf"
